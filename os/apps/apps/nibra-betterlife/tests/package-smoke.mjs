import {_electron as electron,expect} from '@playwright/test';import {mkdtemp,rm} from 'node:fs/promises';import os from 'node:os';import path from 'node:path';
const dir=await mkdtemp(path.join(os.tmpdir(),'nibra-package-'));const app=await electron.launch({executablePath:path.resolve('release/mac-arm64/Nibra BetterLife.app/Contents/MacOS/Nibra BetterLife'),args:[`--user-data-dir=${dir}`]});try{const p=await app.firstWindow();await expect(p.getByRole('heading',{name:'Meet your digital chief of staff.'})).toBeVisible();if(await p.getByText('Start with my workspace',{exact:true}).isVisible()) await p.getByText('Start with my workspace',{exact:true}).click({force:true});await expect(p.getByRole('heading',{name:'Make space for what matters most.'})).toBeVisible();const next=app.waitForEvent('window');await p.evaluate(()=>window.nibra.sidecar());const side=await next;await side.waitForLoadState();await expect(side.getByRole('textbox',{name:'Ask Nibra',exact:true})).toBeVisible();const top=await app.evaluate(({BrowserWindow})=>BrowserWindow.getAllWindows().some(w=>w.isAlwaysOnTop()));expect(top).toBe(true);await side.close();console.log('PASS: packaged macOS app launch, welcome, empty workspace, real separate always-on-top sidecar.');}finally{await app.close();await rm(dir,{recursive:true,force:true})}
